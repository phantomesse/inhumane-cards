'use strict';

const assert = require('assert');
const fs = require('fs');
const cardDatabase = require('../app/card-database.js');

describe('Card database', function() {
  it('Loads in all cards from all card packs', function() {
    assert.strictEqual(true, true);
  })
});
